const solution1 = (strs) => {
    if (strs.length == 0) {
        return "";
    }

    if (strs.length == 1) {
        return strs[0];
    }

    for (let i = 0; i < strs.length; i++) {
        if (strs[i].length == 0) {
            return "";
        }
    }

    for  (let i = 0; i<strs[0]; i++) {
        let l = strs[0][i]
        for (let j = 0; j < strs.length; i++) {
            if (i == )
        }
    }
}